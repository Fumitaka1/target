<div class="wrapper flex">
  <div class="home-outer">
    <%= will_paginate @bookmarks, previous_label: '<', next_label: '>', inner_window: 1 %>
    <% @bookmarks.each do |bookmark| %>
      <div class="post">
      <div class="post_info">
        <%= link_to image_tag("icon.png", alt: "user icon", class: "user-icon"), "" %>
        <div class="post_info_item">
          <h2 class="post_title"><%= link_to bookmark.post.title, post_path(bookmark.post) %></h2>
          <div class="post_info_nav">
            <p class="user_name"><%= link_to bookmark.user.name, user_path(bookmark.user) %></p>
            <p><%= bookmark.post.updated_at.to_s(:datetime_jp) %>更新</p>
          </div>
        </div>
      </div>
    </div>
    <% end %>
    <%= will_paginate @bookmarks, previous_label: '<', next_label: '>', inner_window: 1 %>
  </div>
</div>
